package chapters.Chapter13.Checkpoints13;

public class checkpoint13_03 {
    //An abstract class can be used just like a nonabstract class except that you cannot use the new operator to create an instance from the abstract class.TRUE
    // An abstract class can be extended.TRUE
    //A subclass of a nonabstract superclass cannot be abstract.FALSE
    // A subclass cannot override a concrete method in a superclass to define it as abstract.
    // An abstract method must be nonstatic TRUE
}
