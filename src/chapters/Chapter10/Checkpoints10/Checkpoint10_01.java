package chapters.Chapter10.Checkpoints10;

public class Checkpoint10_01 {
    /**
     * there are 3 requirements for being immutable all data fields must be private and  there can't be any
     * mutator methods for data fields and no accessor methods can return a reference to data field that is mutable !!!,
     * so that the Loan Class cannot be immutable because of returning the reference of Date object.
     */
}
