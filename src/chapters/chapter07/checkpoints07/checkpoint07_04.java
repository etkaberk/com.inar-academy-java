package chapters.chapter07.checkpoints07;

public class checkpoint07_04 {
    //every element in an array has the same type (true)
    //the array size is fixed after an array reference variable is declared (true)
    // the array size is fixed after it is created(false)
    // the elements in an array must be a primitive data type (
}
